/*
void dateNow(){
  
  rtc.getDate(year, month, day, weekday);
  rtc.getTime(hour, minute, second, period);
  if (!(second % 3)) 
    rtc.setMode(1 - rtc.getMode());
  
  rtc.getTime(hour, minute, second, period);
  
  // Data
  timestamp="";
  
  timestamp+=day;
  timestamp+="/";
  timestamp+=month;
  timestamp+="/";
  timestamp+=year + 2000;
  timestamp+=";";
  timestamp+=hour;
  timestamp+=":";
  timestamp+=minute;
  timestamp+=":";
  timestamp+=second;
  //timestamp+=rtc.getMode() ? (period ? "PM" :" AM") : "";
  
}
*/
